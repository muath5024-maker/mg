# 🔐 دليل إعداد GitHub Authentication

## ✅ ما تم إنجازه:
- ✓ تحديث git config (username & email)
- ✓ تحديث remote URLs
- ✓ مسح بيانات اعتماد Git القديمة
- ✓ فتح Credential Manager

---

## 📝 الخطوات المتبقية:

### الخطوة 1: حذف البيانات القديمة من Credential Manager (يدوياً)

**Credential Manager مفتوح الآن أمامك:**

1. ابحث عن: **"GitHub - https://api.github.com/mbuy1"**
2. اضغط عليه
3. اضغط **Remove** أو **حذف**
4. أغلق Credential Manager

---

### الخطوة 2: إنشاء Personal Access Token (PAT)

**افتح المتصفح واذهب إلى:**
```
https://github.com/settings/tokens
```

**الخطوات:**
1. اضغط **"Generate new token"** → **"Generate new token (classic)"**
2. أدخل اسم التوكن: `saleh-project-2025`
3. اختر Expiration: `90 days` أو `No expiration` (حسب تفضيلك)
4. حدد Scopes:
   - ✓ **repo** (full control of private repositories)
5. اضغط **"Generate token"**
6. **⚠️ مهم جداً:** انسخ التوكن فوراً وأحفظه في مكان آمن
   - لن تتمكن من رؤيته مرة أخرى!

---

### الخطوة 3: اختبار المصادقة

بعد إنشاء التوكن، قم بتشغيل هذا الأمر:

```powershell
cd c:\muath\saleh
git push -u origin main
```

**عندما يطلب منك Git:**
- **Username:** `muath5024-maker`
- **Password:** [الصق التوكن الذي نسخته]

**⚠️ ملاحظة:** لا تستخدم كلمة مرور حسابك على GitHub - استخدم التوكن فقط!

---

### الخطوة 4: حفظ التوكن بشكل آمن (اختياري)

يمكنك حفظ التوكن في ملف آمن:

```powershell
# احفظ التوكن في ملف محمي
"YOUR_TOKEN_HERE" | Out-File -FilePath "$env:USERPROFILE\.github-token" -Encoding UTF8

# لقراءته لاحقاً:
Get-Content "$env:USERPROFILE\.github-token"
```

---

## 🔍 التحقق من النجاح

بعد نجاح `git push`، تحقق من:

1. **على GitHub:**
   - اذهب إلى: https://github.com/muath5024-maker/saleh
   - يجب أن ترى جميع ملفاتك

2. **محلياً:**
   ```bash
   git log --oneline -5
   git remote -v
   ```

---

## ❓ استكشاف الأخطاء

### خطأ: "Repository not found"
- تأكد من إنشاء المستودع على GitHub أولاً
- تحقق من اسم المستودع (saleh)

### خطأ: "Authentication failed"
- تأكد من استخدام التوكن وليس كلمة المرور
- تأكد من أن التوكن له صلاحية `repo`
- تحقق من Username: `muath5024-maker`

### خطأ: "Permission denied"
- تأكد من أن المستودع ملكك أو لديك صلاحيات push

---

## 🎯 ملخص سريع

```bash
# 1. حذف البيانات القديمة (يدوياً من Credential Manager)
# 2. إنشاء PAT من GitHub
# 3. اختبار:
cd c:\muath\saleh
git push -u origin main
# Username: muath5024-maker
# Password: [PAT التوكن]
```

---

**✨ نصيحة:** احفظ التوكن في مدير كلمات مرور آمن مثل:
- 1Password
- LastPass
- Bitwarden
- Windows Credential Manager (بعد نجاح أول push)
