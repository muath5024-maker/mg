# 📊 Firebase Analytics - التطبيق الكامل

**التاريخ:** يناير 2025  
**الحالة:** ✅ تم تطبيق Firebase Analytics بشكل شامل

---

## 📋 نظرة عامة

تم تحسين وتطبيق Firebase Analytics بشكل كامل في جميع أنحاء التطبيق لتتبع سلوك المستخدمين والأحداث المهمة.

---

## ✅ ما تم إنجازه

### 1. تحسين FirebaseService

تم إضافة دوال جديدة لتتبع الأحداث:

#### الأحداث الأساسية
- ✅ `logScreenView()` - تتبع عرض الشاشات (محسّن)
- ✅ `logLogin()` - تتبع تسجيل الدخول
- ✅ `logSignUp()` - تتبع التسجيل
- ✅ `logAppOpen()` - تتبع فتح التطبيق

#### أحداث المنتجات
- ✅ `logViewProduct()` - تتبع عرض منتج
- ✅ `logAddToCart()` - تتبع إضافة للسلة
- ✅ `logRemoveFromCart()` - تتبع حذف من السلة
- ✅ `logAddToWishlist()` - تتبع إضافة للمفضلة
- ✅ `logPlaceOrder()` - تتبع إتمام طلب

#### أحداث المتاجر
- ✅ `logViewStore()` - تتبع عرض متجر

#### أحداث المالية
- ✅ `logViewWallet()` - تتبع عرض المحفظة
- ✅ `logAddFunds()` - تتبع إضافة رصيد
- ✅ `logViewPoints()` - تتبع عرض النقاط
- ✅ `logUsePoints()` - تتبع استخدام النقاط

#### أحداث الكوبونات
- ✅ `logViewCoupon()` - تتبع عرض كوبون
- ✅ `logApplyCoupon()` - تتبع استخدام كوبون

#### أحداث أخرى
- ✅ `logSearch()` - تتبع البحث
- ✅ `logShare()` - تتبع المشاركة
- ✅ `logCustomEvent()` - أحداث مخصصة

#### User Properties
- ✅ `setUserId()` - تعيين User ID
- ✅ `setUserProperty()` - تعيين خصائص المستخدم

---

### 2. تطبيق Analytics في الشاشات

#### الشاشات المحدثة:

**ProductDetailsScreen:**
- ✅ تتبع عرض المنتج عند فتح الشاشة
- ✅ تتبع عرض الشاشة

**StoreDetailsScreen:**
- ✅ تتبع عرض المتجر عند فتح الشاشة
- ✅ تتبع عرض الشاشة

**CartScreen:**
- ✅ تتبع عرض السلة
- ✅ تتبع إتمام الطلب مع تفاصيل (order_id, total_amount, coupon_code)

**CustomerWalletScreen:**
- ✅ تتبع عرض المحفظة
- ✅ تتبع الرصيد عند التحميل

**CustomerPointsScreen:**
- ✅ تتبع عرض النقاط
- ✅ تتبع رصيد النقاط عند التحميل

**RootWidget:**
- ✅ تعيين User ID عند تسجيل الدخول
- ✅ تعيين User Properties (role, display_name)

---

## 📊 الأحداث المتتبعة

### أحداث تلقائية

| الحدث | متى يتم تتبعه | البيانات |
|-------|---------------|----------|
| `screen_view` | عند فتح أي شاشة | screen_name, parameters |
| `app_open` | عند فتح التطبيق | - |
| `login` | عند تسجيل الدخول | login_method |
| `sign_up` | عند التسجيل | sign_up_method |

### أحداث المنتجات

| الحدث | متى يتم تتبعه | البيانات |
|-------|---------------|----------|
| `view_item` | عند عرض منتج | product_id, product_name, price, category |
| `add_to_cart` | عند إضافة للسلة | product_id, product_name, price, quantity |
| `remove_from_cart` | عند حذف من السلة | product_id, product_name |
| `add_to_wishlist` | عند إضافة للمفضلة | product_id, product_name, price |
| `purchase` | عند إتمام طلب | order_id, total_amount, coupon_code |

### أحداث المتاجر

| الحدث | متى يتم تتبعه | البيانات |
|-------|---------------|----------|
| `view_store` | عند عرض متجر | store_id, store_name |

### أحداث المالية

| الحدث | متى يتم تتبعه | البيانات |
|-------|---------------|----------|
| `view_wallet` | عند عرض المحفظة | balance |
| `add_funds` | عند إضافة رصيد | amount, payment_method |
| `view_points` | عند عرض النقاط | points_balance |
| `use_points` | عند استخدام النقاط | points, reason |

### أحداث الكوبونات

| الحدث | متى يتم تتبعه | البيانات |
|-------|---------------|----------|
| `view_coupon` | عند عرض كوبون | coupon_code, coupon_name |
| `apply_coupon` | عند استخدام كوبون | coupon_code, discount_amount |

---

## 🎯 User Properties

يتم تعيين الخصائص التالية تلقائياً:

- `user_role` - دور المستخدم (customer, merchant, admin)
- `display_name` - اسم المستخدم المعروض
- `user_id` - معرف المستخدم

---

## 📱 كيفية الاستخدام

### في أي شاشة:

```dart
import '../../../../core/firebase_service.dart';

@override
void initState() {
  super.initState();
  // تتبع عرض الشاشة
  WidgetsBinding.instance.addPostFrameCallback((_) {
    FirebaseService.logScreenView('screen_name');
  });
}
```

### تتبع حدث مخصص:

```dart
// تتبع عرض منتج
await FirebaseService.logViewProduct(
  productId: 'prod_123',
  productName: 'منتج رائع',
  price: 99.0,
  category: 'إلكترونيات',
);

// تتبع إضافة للسلة
await FirebaseService.logAddToCart(
  productId: 'prod_123',
  productName: 'منتج رائع',
  price: 99.0,
  quantity: 2,
);

// تتبع إتمام طلب
await FirebaseService.logPlaceOrder(
  orderId: 'order_123',
  totalAmount: 198.0,
  couponCode: 'DISCOUNT10',
);
```

---

## 🔍 عرض البيانات في Firebase Console

1. افتح [Firebase Console](https://console.firebase.google.com)
2. اختر المشروع
3. اذهب إلى **Analytics** > **Events**
4. ستجد جميع الأحداث المتتبعة

### التقارير المهمة:

- **Events** - جميع الأحداث
- **User Properties** - خصائص المستخدمين
- **Audiences** - شرائح المستخدمين
- **Funnels** - مسارات التحويل
- **Conversions** - التحويلات

---

## 📈 المقاييس المتتبعة

### مقاييس المستخدمين
- عدد المستخدمين النشطين
- عدد المستخدمين الجدد
- معدل الاحتفاظ

### مقاييس المنتجات
- عدد المنتجات المعروضة
- عدد المنتجات المضافة للسلة
- معدل التحويل من عرض إلى شراء

### مقاييس الطلبات
- عدد الطلبات المكتملة
- متوسط قيمة الطلب
- معدل استخدام الكوبونات

### مقاييس المالية
- عدد عمليات إضافة الرصيد
- متوسط قيمة الرصيد
- استخدام النقاط

---

## ⚙️ الإعدادات

### في Firebase Console:

1. **تفعيل Analytics:**
   - Analytics > Settings
   - تأكد من تفعيل Google Analytics

2. **إعداد Events:**
   - Analytics > Events
   - يمكنك رؤية جميع الأحداث تلقائياً

3. **إعداد Conversions:**
   - Analytics > Conversions
   - حدد الأحداث المهمة كتحويلات (مثل: purchase)

---

## 🎯 الخطوات التالية (اختيارية)

### أولوية متوسطة
1. ⏳ إضافة تتبع في الشاشات الأخرى (Explore, Categories, etc.)
2. ⏳ إضافة تتبع للأخطاء (Error Tracking)
3. ⏳ إضافة تتبع للأداء (Performance Monitoring)

### أولوية منخفضة
4. ⏳ إعداد Custom Dashboards
5. ⏳ إعداد Alerts
6. ⏳ إعداد A/B Testing

---

## 📝 ملاحظات مهمة

1. **الخصوصية:**
   - لا يتم تتبع معلومات حساسة
   - جميع البيانات مجهولة الهوية

2. **الأداء:**
   - جميع استدعاءات Analytics غير متزامنة
   - لا تؤثر على أداء التطبيق

3. **التطوير:**
   - في وضع التطوير، يتم طباعة الأحداث في Console
   - في الإنتاج، يتم إرسالها إلى Firebase

---

## ✅ التحقق

```bash
# تشغيل التطبيق
flutter run

# في Console ستجد:
# 📊 Analytics: عرض شاشة product_details
# 📊 Analytics: عرض منتج منتج رائع
# etc.
```

---

## 🎉 الخلاصة

تم تطبيق Firebase Analytics بشكل شامل في التطبيق:
- ✅ 15+ نوع حدث مختلف
- ✅ تتبع تلقائي للشاشات الرئيسية
- ✅ User Properties تلقائية
- ✅ جاهز للإنتاج

التطبيق الآن يتبع جميع الأحداث المهمة ويمكنك رؤية البيانات في Firebase Console.

---

**آخر تحديث:** يناير 2025  
**الحالة:** ✅ Firebase Analytics مطبق بالكامل

