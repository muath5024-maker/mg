# حماية الترميز العربي - دليل المطورين

## ⚠️ تحذير مهم

هذا المشروع يحتوي على نصوص عربية. يجب الالتزام بالقواعد التالية لتجنب مشاكل الترميز:

## ✅ القواعد الإلزامية

### 1. استخدام VS Code فقط
- **لا تستخدم** محررات نصوص قديمة (Notepad, Notepad++, إلخ)
- استخدم **VS Code** أو **Android Studio** فقط
- تأكد من تثبيت إضافة **Dart** و **Flutter**

### 2. التحقق من إعدادات الترميز
تأكد أن إعدادات VS Code تحتوي على:
```json
{
  "files.encoding": "utf8",
  "files.autoGuessEncoding": false,
  "files.eol": "\n"
}
```

### 3. قبل عمل Commit
قم بفحص الملفات المعدلة:
```bash
git diff
```
تأكد أن النصوص العربية **مقروءة** وليست رموز مثل: `Ø§Ù„Ø¥Ø´Ø¹Ø§Ø±Ø§Øª`

### 4. إذا ظهرت نصوص مخربة
**لا تحفظ الملف!** 
- استخدم `git checkout -- <filename>` لاستعادة النسخة الأصلية
- أعد فتح VS Code وتأكد من الإعدادات
- أعد التعديل

## 🔧 الملفات المهمة

تم إضافة الملفات التالية لحماية الترميز:
- `.gitattributes` - يحدد كيفية تعامل Git مع الملفات
- `.editorconfig` - يحدد إعدادات المحرر
- `.vscode/settings.json` - إعدادات VS Code المحلية

## 📝 الملفات التي تحتوي على نصوص عربية

انتبه خصوصاً عند تعديل:
- `lib/features/dashboard/presentation/screens/notifications_screen.dart`
- `lib/features/dashboard/presentation/screens/shortcuts_screen.dart`
- `lib/shared/widgets/error_boundary.dart`
- `lib/features/settings/presentation/screens/*.dart`

## 🚫 ممنوع منعاً باتاً

1. ❌ فتح ملفات `.dart` في Notepad
2. ❌ نسخ نصوص من Word أو PDF
3. ❌ تغيير encoding الملف في المحرر
4. ❌ استخدام Git clients قديمة

## ✓ الطريقة الصحيحة

1. ✅ افتح المشروع في VS Code
2. ✅ عدل الملف مباشرة في VS Code
3. ✅ احفظ بـ Ctrl+S
4. ✅ تحقق من `git diff` قبل الـ commit
5. ✅ اعمل commit و push

---

**ملاحظة:** إذا واجهت أي مشكلة ترميز، اتصل بمدير المشروع فوراً قبل عمل commit.
