# 🎯 دليل استخدام AnythingLLM مع مشروعك

## 📊 الوضع الحالي:

### ✅ ما تم:

- ✅ AnythingLLM يعمل على http://localhost:3001
- ✅ ملفات المشروع موجودة في `/app/collector/hotdir/mbuy-project/`
- ✅ المشروع يحتوي على:
  - Flutter files (في مجلد `flutter/`)
  - Worker files (في مجلد `worker/`)
  - README.md

### ❌ ما ينقص:

- ❌ **الملفات لم تُفهرس بعد** - AnythingLLM لا يعرف محتوياتها
- ❌ لم يتم إضافتها إلى Workspace

---

## 🚀 خطوات تفعيل AnythingLLM ليعرف مشروعك:

### الخطوة 1️⃣: فتح AnythingLLM

افتح في المتصفح: http://localhost:3001

### الخطوة 2️⃣: إنشاء أو اختيار Workspace

1. **اذهب إلى الصفحة الرئيسية**
2. **ستجد Workspaces:**
   - إذا كان هناك workspace اسمه **"luh"** → اختره
   - إذا لم يكن موجود → اضغط **"New Workspace"**
   - سمّه: **"mbuy-project"**

### الخطوة 3️⃣: إضافة المستندات

داخل الـ Workspace:

1. **ابحث عن زر "Upload Documents"** أو **"Add Documents"**
   - غالباً في الأعلى أو في القائمة الجانبية

2. **اختر طريقة الرفع:**

   **الطريقة أ: من خلال الواجهة**

   ```
   → Upload Documents
   → اختر "From Directory" أو "Folder"
   → حدد المسار: /app/collector/hotdir/mbuy-project
   ```

   **الطريقة ب: السحب والإفلات**

   ```
   → افتح مجلد المشروع على جهازك
   → اسحب الملفات إلى نافذة AnythingLLM
   ```

   **الطريقة ج: استخدام GitHub (الأفضل!)**

   ```
   → Upload Documents
   → اختر "From GitHub"
   → أدخل: https://github.com/muath5024-maker/mg
   → Branch: main
   → اضغط "Import"
   ```

### الخطوة 4️⃣: انتظر الفهرسة

- ستبدأ AnythingLLM بقراءة وفهرسة الملفات
- قد يستغرق دقائق حسب حجم المشروع
- ستظهر رسالة "Processing..." أو "Embedding..."

### الخطوة 5️⃣: ابدأ الاستخدام!

بعد انتهاء الفهرسة:

```
✅ الآن AnythingLLM يعرف كل شيء عن مشروعك!
```

---

## 💬 أمثلة على الأسئلة:

بعد الفهرسة، يمكنك سؤال:

### عن البنية:

```
❓ "ما هي الملفات الرئيسية في مشروع Flutter؟"
❓ "كيف منظم مشروع mbuy-worker؟"
❓ "ما هي المكتبات المستخدمة في pubspec.yaml؟"
```

### عن الكود:

```
❓ "كيف يعمل auth_controller.dart؟"
❓ "شرح لي طريقة التعامل مع API في المشروع"
❓ "ما هي routes المتاحة في Flutter app؟"
```

### عن التطوير:

```
❓ "كيف أضيف feature جديد؟"
❓ "أين أجد كود الـ cart؟"
❓ "كيف أربط Flutter app بـ Worker API؟"
```

---

## 🎨 الطريقة السريعة (الآن):

### استخدم GitHub Integration:

1. **افتح:** http://localhost:3001
2. **اذهب إلى Workspace**
3. **اضغط:** "Upload Documents" → "From GitHub"
4. **أدخل:**
   ```
   Repository: https://github.com/muath5024-maker/mg
   Branch: main
   ```
5. **اضغط "Import"** أو "Sync"
6. **انتظر الفهرسة**
7. **ابدأ السؤال!**

---

## 🔄 المزامنة التلقائية:

بعد الإعداد الأولي، يمكنك تفعيل:

### في Workspace Settings:

```
✅ Auto-sync with GitHub
✅ Sync on push
✅ Update every: 1 hour
```

الآن كل ما تدفعه إلى GitHub سيُحدث تلقائياً في AnythingLLM!

---

## 📱 طرق الاستخدام:

### 1. من المتصفح (الأسهل):

```
http://localhost:3001
→ افتح Workspace
→ اكتب سؤالك في Chat
→ احصل على إجابة فورية
```

### 2. من VS Code (بعد تثبيت Extension):

```
Ctrl + Shift + P
→ "Anything LLM: Ask Question"
→ اكتب سؤالك
→ احصل على إجابة مع سياق الكود
```

### 3. عبر API (للمطورين):

```bash
curl -X POST http://localhost:3001/api/v1/workspace/chat \
  -H "Content-Type: application/json" \
  -d '{
    "message": "ما هي ملفات Flutter الرئيسية؟",
    "mode": "query"
  }'
```

---

## ⚡ اختبار سريع:

### جرب الآن:

1. افتح: http://localhost:3001
2. اذهب إلى Workspace
3. في Chat Box اكتب:
   ```
   "مرحباً، هل تستطيع رؤية ملفات مشروعي؟"
   ```

**إذا قال:**

- ✅ "نعم، أرى ملفات Flutter/Worker..." → **تمام! الفهرسة تمت**
- ❌ "لا أستطيع..." → **يحتاج رفع الملفات (الخطوة 3)**

---

## 🛠️ طريقة بديلة (من Terminal):

إذا كنت تفضل السرعة:

```powershell
# انسخ المشروع كاملاً إلى مجلد يراه AnythingLLM
docker exec anythingllm mkdir -p /workspace/mg
docker cp C:\mg\. anythingllm:/workspace/mg/

# ثم في AnythingLLM:
# Upload Documents → From Path → /workspace/mg
```

---

## 📊 ما الذي سيعرفه AnythingLLM بعد الفهرسة:

```
✅ جميع ملفات Flutter (.dart)
✅ جميع ملفات Worker (.ts, .js)
✅ ملفات الإعداد (pubspec.yaml, package.json, etc.)
✅ الوثائق (.md files)
✅ البنية والعلاقات بين الملفات
✅ التعليقات والشروحات في الكود
✅ Dependencies والمكتبات المستخدمة
```

**لن يعرف:**

```
❌ الأسرار في .env (وهذا جيد!)
❌ ملفات build/ (مستثناة)
❌ node_modules/ (مستثناة)
```

---

## 🎓 نصائح للاستخدام الأمثل:

### 1. كن محدداً في أسئلتك:

```
❌ "كيف يعمل المشروع؟"
✅ "كيف يتم authentication في Flutter app؟"
```

### 2. اطلب أمثلة:

```
✅ "أعطني مثال على استخدام CartProvider"
```

### 3. اسأل عن التحسينات:

```
✅ "كيف يمكن تحسين performance في ProductsScreen؟"
```

### 4. اطلب كود جديد:

```
✅ "اكتب لي widget جديد لعرض قائمة المنتجات"
```

---

## 🔧 استكشاف الأخطاء:

### المشكلة: "لا يرى الملفات"

**الحل:**

```
1. تأكد من رفع المستندات (الخطوة 3)
2. انتظر انتهاء الفهرسة
3. تحقق من Workspace Settings → Documents
```

### المشكلة: "إجابات غير دقيقة"

**الحل:**

```
1. أعد صياغة السؤال بشكل أدق
2. حدد اسم الملف المطلوب
3. أضف سياق أكثر في السؤال
```

### المشكلة: "بطء في الاستجابة"

**الحل:**

```
1. قلل حجم المستندات المفهرسة
2. استخدم Ollama models محلية أخف
3. زد موارد Docker (RAM, CPU)
```

---

## ✅ الخلاصة - ماذا تفعل الآن:

### الخطوات البسيطة:

1. **افتح:** http://localhost:3001
2. **اذهب إلى:** Workspace (أو أنشئ واحد جديد)
3. **اضغط:** "Upload Documents" → "From GitHub"
4. **أدخل:** `https://github.com/muath5024-maker/mg`
5. **انتظر** الفهرسة (5-10 دقائق)
6. **ابدأ السؤال!**

**بعدها AnythingLLM سيعرف كل شيء عن مشروعك! 🎉**

---

**هل تحتاج مساعدة في أي خطوة؟ أخبرني!**
