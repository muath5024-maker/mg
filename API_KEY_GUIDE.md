# 🔑 كيفية الحصول على API Key من AnythingLLM

## الخطوات التفصيلية:

### الطريقة 1️⃣: عبر الواجهة الرئيسية

1. **افتح AnythingLLM:**

   ```
   http://localhost:3001
   ```

2. **ابحث عن أيقونة الإعدادات:**
   - في الزاوية اليسرى السفلية
   - أيقونة ⚙️ (Settings/Gear icon)
   - أو في القائمة الجانبية

3. **اضغط على Settings/الإعدادات**

4. **في صفحة Settings، ابحث عن أحد هذه الخيارات:**
   - **API Keys** (في القائمة الجانبية اليسرى)
   - أو **Developer** أو **Integration**
   - أو **Security**

5. **اضغط على "API Keys"**

6. **ستجد زر:**
   - "Create New API Key" أو
   - "Generate API Key" أو
   - "+ New API Key"

7. **اضغط على الزر وانسخ المفتاح**

---

### الطريقة 2️⃣: إذا لم تجد قسم API Keys

بعض إصدارات AnythingLLM قد لا تحتاج API Key للاستخدام المحلي!

**جرب هذا:**

1. **اترك API Key فارغاً في الإعدادات**
2. **استخدم Extension مباشرة**
3. **سيتصل تلقائياً بدون API Key**

---

### الطريقة 3️⃣: التحقق من الإصدار

```powershell
# تحقق من إصدار AnythingLLM
docker exec anythingllm cat /app/package.json | findstr version
```

بعض الإصدارات القديمة لا تدعم API Keys.

---

### الطريقة 4️⃣: استخدام بدون API Key

إذا لم تجد API Keys، جرب هذا الإعداد:

**في `.vscode/settings.json`:**

```json
{
  "anything-llm.apiKey": "", // اتركه فارغاً
  "anything-llm.baseURL": "http://localhost:3001",
  "anything-llm.workspace": "luh",
  "anything-llm.enabled": true,
  "anything-llm.requireAuth": false // أضف هذا
}
```

---

## 🎯 خطوات بديلة للتجربة الآن:

### استخدم AnythingLLM مباشرة من المتصفح:

1. **افتح:** http://localhost:3001
2. **اختر workspace "luh"** (إذا كان موجوداً)
3. **ابدأ الدردشة مباشرة!**
4. **اسأل أي سؤال عن المشروع**

لا تحتاج VS Code Extension للبدء!

---

## 🔍 أين تبحث بالضبط:

عند فتح http://localhost:3001:

```
┌─────────────────────────────────────┐
│  AnythingLLM                    ⚙️  │ ← اضغط هنا
├─────────────────────────────────────┤
│  Workspaces                         │
│  Documents                          │
│  Settings  ← ابحث عن هذا           │
│    ├─ General                       │
│    ├─ API Keys  ← أو هذا           │
│    ├─ Security                      │
│    ├─ Integrations                  │
│    └─ Developer  ← أو هذا          │
└─────────────────────────────────────┘
```

---

## ⚡ الحل السريع:

**جرب Extension بدون API Key:**

1. **ثبت Extension:**
   - Ctrl + Shift + X
   - ابحث: "Anything LLM"
   - Install

2. **اتركه يتصل تلقائياً بـ:**
   - http://localhost:3001

3. **جربه:**
   - Ctrl + Shift + P
   - "Anything LLM: Ask Question"
   - اكتب سؤالك

---

## 💡 ملاحظة مهمة:

**AnythingLLM المحلي قد لا يحتاج API Key!**

API Keys مهمة فقط عند:

- الاستخدام عبر الإنترنت
- ربط تطبيقات خارجية
- إنتاج/Production environments

للاستخدام المحلي على localhost، غالباً **لا تحتاج API Key**.

---

## 🆘 إذا مازلت لا تجده:

أرسل لي screenshot من:

1. صفحة http://localhost:3001
2. قائمة Settings

وسأساعدك بالضبط أين تجده!
